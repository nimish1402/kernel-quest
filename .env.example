# Supabase Configuration
# These credentials work for both development and production
# The auth system will automatically detect the environment
#
# ⚠️  IMPORTANT: Configure these URLs in Supabase Dashboard:
# 1. Go to Authentication → URL Configuration
# 2. Site URL: https://your-production-domain.com
# 3. Redirect URLs: 
#    - http://localhost:3000/auth/callback
#    - https://your-production-domain.com/auth/callback

# Get these from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Override site URL (auto-detected if not provided)
# Development: http://localhost:3000
# Production: https://your-production-domain.com
# 
# 🔧 FOR DEVELOPMENT TESTING: Uncomment the line below to force localhost
# 🚨 FOR PRODUCTION: Make sure the line below is COMMENTED OUT!
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe Configuration (if using payment features)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
# STRIPE_SECRET_KEY=your-stripe-secret-key

# Other optional environment variables
# DATABASE_URL=your-database-url
# NEXTAUTH_SECRET=your-nextauth-secret
# NEXTAUTH_URL=http://localhost:3000
